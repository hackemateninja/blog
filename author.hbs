{{!< default}}

<header class="blog-header" {{#if author.cover}}style="background-image: url({{author.cover}})"{{/if}}>
    <div class="vertical">
        <h1>Artículos de: {{author.name}}</h1>
    </div>
</header>

<div class="wrap">
    <section class="blog-content">

        {{#foreach posts}}

        <article class="{{post_class}}">
            <header class="post-header">
                <h2 class="post-title"><a href="{{url}}">{{{title}}}</a></h2>
                <section class="post-meta">
                    <img class="author-img" src="{{author.image}}" alt="{{author.name}}" nopin="nopin" />
                    <span class="author-name">{{author}}</span>
                    &bull;
                    <time datetime="{{date format='YYYY-MM-DD'}}">{{date format="D MMM YYYY"}}</time>
                    <span class="tags">{{tags prefix="• Tags: "}}</span>
                </section>
            </header>
            <section class="post-excerpt">
                <p>{{excerpt words="55"}}&hellip; <a class="read-more" href="{{url}}">Leer más</a></p>
            </section>
        </article>

        {{/foreach}}

        {{pagination}}

    </section>

    {{> sidebar}}

</div>
